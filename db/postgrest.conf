db-uri = "postgres://authenticator:postgres@localhost:5432/landingPage"
db-schemas = "api"
db-anon-role = "web_anon"
server-port = 3001
# Content tables are exposed as read-only views (see *_public views in schema.sql).
# Writes (newsletter signup, analytics) always go through FastAPI, never straight
# through PostgREST, so there is no writable anon role here.
