createdb myb && psql -d myb -f db/schema.sql        # 1. database
postgrest db/postgrest.conf                          # 2. PostgREST on :3001
cd backend && uv sync && uv run uvicorn app.main:app --reload --port 8000   # 3. API on :8000
cd frontend && npm install && npm run dev             # 4. Vite on :5173